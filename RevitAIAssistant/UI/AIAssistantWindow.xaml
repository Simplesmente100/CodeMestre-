<Window x:Class="RevitAIAssistant.UI.AIAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit AI Assistant - Powered by Meta Llama 3.3 70B" 
        Height="600" Width="800" 
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Icon="/RevitAIAssistant;component/Resources/ai_icon.ico">
    
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF388E3C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF1B5E20"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFBDBDBD"/>
                                <Setter Property="Foreground" Value="#FF757575"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChatBubbleUser" TargetType="Border">
            <Setter Property="Background" Value="#FF1976D2"/>
            <Setter Property="CornerRadius" Value="15,15,5,15"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="50,5,10,5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="ChatBubbleAssistant" TargetType="Border">
            <Setter Property="Background" Value="#FFE0E0E0"/>
            <Setter Property="CornerRadius" Value="15,15,15,5"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="10,5,50,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2E7D32" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🤖" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="AI Assistant for Revit" 
                                 FontSize="16" FontWeight="Bold" 
                                 Foreground="White"/>
                        <TextBlock Text="Powered by Together.ai Meta Llama 3.3 70B Instruct Turbo" 
                                 FontSize="11" 
                                 Foreground="#FFCCCCCC"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Name="ClearChatButton" Content="🗑️ Clear" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#FFFF5722"
                            Click="ClearChatButton_Click"
                            ToolTip="Clear chat history"/>
                    <Button Name="SettingsButton" Content="⚙️ Settings" 
                            Style="{StaticResource ModernButtonStyle}"
                            Background="#FF607D8B"
                            Click="SettingsButton_Click"
                            ToolTip="Open settings"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Chat Area -->
        <ScrollViewer Grid.Row="1" 
                      Name="ChatScrollViewer" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="#FFF8F9FA">
            <StackPanel Name="ChatPanel" Margin="10">
                <Border Style="{StaticResource ChatBubbleAssistant}" Margin="10,10,50,5">
                    <TextBlock Text="Hello! I'm your AI assistant for Revit. I can help you with modeling techniques, troubleshooting, API questions, and more. How can I assist you today?" 
                             TextWrapping="Wrap"
                             FontSize="14"
                             Foreground="#FF212121"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Context Info -->
        <Expander Grid.Row="2" 
                  Header="📋 Current Revit Context" 
                  Background="#FFF0F0F0"
                  Margin="10,5">
            <TextBlock Name="ContextTextBlock" 
                     Text="Loading Revit context..."
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="11"
                     Margin="10"
                     Background="White"
                     Padding="10"/>
        </Expander>

        <!-- Input Area -->
        <Border Grid.Row="3" Background="White" BorderBrush="#FFDDDDDD" BorderThickness="0,1,0,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Name="MessageTextBox" 
                         Grid.Row="0" Grid.Column="0"
                         MinHeight="60"
                         MaxHeight="120"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Padding="12"
                         FontSize="14"
                         BorderBrush="#FFDDDDDD"
                         BorderThickness="1"
                         Background="#FFFAFAFA"
                         KeyDown="MessageTextBox_KeyDown"
                         Text="Type your question about Revit here...">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#FF2E7D32"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Name="SendButton" 
                        Grid.Row="0" Grid.Column="1"
                        Content="📤 Send" 
                        Style="{StaticResource ModernButtonStyle}"
                        Click="SendButton_Click"
                        VerticalAlignment="Bottom"
                        MinWidth="100"
                        IsDefault="True"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" 
                          Orientation="Horizontal" 
                          Margin="0,10,0,0">
                    <CheckBox Name="IncludeContextCheckBox" 
                            Content="Include Revit context in message" 
                            IsChecked="True"
                            VerticalAlignment="Center"
                            Margin="0,0,15,0"/>
                    <TextBlock Name="StatusTextBlock" 
                             Text="Ready" 
                             VerticalAlignment="Center"
                             Foreground="#FF666666"
                             FontStyle="Italic"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>